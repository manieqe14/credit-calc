import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#a6ebc9ff',
    },
    grey: {
      100: 'rgba(0, 0, 0, 0.1)',
    },
    text: {
      primary: 'rgb(37, 40, 42)',
    },
  },
});
